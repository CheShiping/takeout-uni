# 第十一章：地址管理

## 11.1 地址数据模型

```typescript
// src/models/address.types.ts

export interface UserAddress {
  id: string
  userId: string
  consignee: string          // 收货人
  phone: string
  province: string
  city: string
  district: string
  detail: string             // 详细地址
  longitude: number
  latitude: number
  isDefault: number          // 1=默认, 0=非默认
  createTime: string
}

export interface AddAddressRequest {
  consignee: string
  phone: string
  province: string
  city: string
  district: string
  detail: string
  longitude: number
  latitude: number
  isDefault: number
}
```

## 11.2 AddressService

```typescript
// src/services/AddressService.ts

export class AddressService {
  /** 获取地址列表 */
  static async getAddresses(): Promise<UserAddress[]> {
    const response = await httpUtil.get<UserAddress[]>(API_CONFIG.PATHS.ADDRESS_LIST)
    return response.data || []
  }

  /** 新增地址 */
  static async addAddress(request: AddAddressRequest): Promise<UserAddress> {
    const response = await httpUtil.post<UserAddress>(API_CONFIG.PATHS.ADDRESS_ADD, request)
    return response.data
  }

  /** 更新地址 */
  static async updateAddress(request: UserAddress): Promise<UserAddress> {
    const url = API_CONFIG.PATHS.ADDRESS_UPDATE.replace('{id}', String(request.id))
    const response = await httpUtil.put<UserAddress>(url, request)
    return response.data
  }

  /** 删除地址 */
  static async deleteAddress(id: string): Promise<void> {
    const url = API_CONFIG.PATHS.ADDRESS_DELETE.replace('{id}', id)
    await httpUtil.delete(url)
  }

  /** 获取默认地址 */
  static async getDefaultAddress(): Promise<UserAddress | null> {
    const addresses = await AddressService.getAddresses()
    return addresses.find(a => a.isDefault === 1) || addresses[0] || null
  }
}
```

## 11.3 地址列表页面

`pages/address/address-list.vue` 展示用户的所有收货地址。

### 页面结构

```
┌──────────────────────────┐
│  地址卡片 1  [默认]       │
│  张三  13800138000        │
│  北京市朝阳区xxx路xxx号   │
│  [编辑]  [删除]           │
├──────────────────────────┤
│  地址卡片 2               │
│  李四  13900139000        │
│  上海市浦东新区xxx        │
│  [编辑]  [删除]           │
├──────────────────────────┤
│  [+ 新增地址]             │
└──────────────────────────┘
```

### 操作逻辑

```typescript
// 删除地址
async function onDeleteAddress(id: string) {
  const confirmed = await ToastUtil.showConfirm('确认删除', '确定要删除这个地址吗？')
  if (confirmed) {
    await AddressService.deleteAddress(id)
    addresses.value = addresses.value.filter(a => a.id !== id)
    ToastUtil.showSuccess('已删除')
  }
}

// 选择地址（从订单确认页跳转来）
function onSelectAddress(address: UserAddress) {
  const cartStore = useCartStore()
  cartStore.setTakeOutAddr(address.id)
  uni.navigateBack()
}
```

## 11.4 新增/编辑地址

`pages/address/add-address.vue` 使用表单收集地址信息。

### 表单字段

```typescript
const form = reactive({
  consignee: '',
  phone: '',
  province: '',
  city: '',
  district: '',
  detail: '',
  longitude: 0,
  latitude: 0,
  isDefault: 0
})
```

### 表单验证

```typescript
function validateForm(): boolean {
  if (!form.consignee.trim()) {
    ToastUtil.showMessage('请输入收货人姓名')
    return false
  }
  if (!form.phone.trim() || !/^1\d{10}$/.test(form.phone)) {
    ToastUtil.showMessage('请输入正确的手机号')
    return false
  }
  if (!form.detail.trim()) {
    ToastUtil.showMessage('请输入详细地址')
    return false
  }
  return true
}
```

### 提交逻辑

```typescript
async function onSubmit() {
  if (!validateForm()) return

  if (isEdit.value) {
    await AddressService.updateAddress({ ...form, id: editId.value } as UserAddress)
    ToastUtil.showSuccess('修改成功')
  } else {
    await AddressService.addAddress(form as AddAddressRequest)
    ToastUtil.showSuccess('添加成功')
  }
  uni.navigateBack()
}
```

## 11.5 地图选点

使用 `MapUtil` 工具类处理坐标转换。用户可以通过地图选择位置，获取经纬度后自动填充省市区信息。

```typescript
// 坐标转换示例
const gcj02 = MapUtil.wgs84ToGcj02(wgs84Lng, wgs84Lat)
form.longitude = gcj02.longitude
form.latitude = gcj02.latitude
```

## 小结

本章讲解了地址管理模块。核心是**CRUD 完整流程 + 表单验证 + 默认地址逻辑**。地址数据在订单确认时被引用，通过 CartStore 传递选中的地址 ID。下一章我们将讲解用户认证。
