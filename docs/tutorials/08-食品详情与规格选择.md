# 第八章：食品详情与规格选择

## 8.1 食品服务层

`src/services/FoodService.ts` 封装食品相关 API：

```typescript
export class FoodService {
  /** 获取食品列表（自动获取首个店铺） */
  static async getFoods(shopId?: string): Promise<FoodItem[]> {
    if (!shopId) {
      const shops = await ShopService.getShops()
      if (shops.length > 0) shopId = shops[0].id
    }
    const params: Record<string, unknown> = {}
    if (shopId) params.shopId = shopId
    const response = await httpUtil.get<FoodItem[]>(API_CONFIG.PATHS.FOOD_LIST, params)
    return (response.data || []).filter(food => food.status === 'on')
  }

  /** 获取分类列表（包含食品数据） */
  static async getCategories(shopId: string): Promise<FoodCategory[]> {
    const response = await httpUtil.get<FoodCategory[]>(API_CONFIG.PATHS.FOOD_CATEGORIES, { shopId })
    return response.data || []
  }

  /** 按分类获取食品 */
  static async getFoodsByCategory(shopId: string): Promise<FoodListByCategory[]> {
    const categories = await FoodService.getCategories(shopId)
    return categories.map(cat => ({
      categoryId: cat.id,
      categoryName: cat.name,
      foods: (cat.foods || []).filter(f => f.status === 'on')
    })).filter(item => item.foods.length > 0)
  }
}
```

关键设计：`getFoodsByCategory` 从分类接口获取数据，后端已按分类分组返回，前端只需过滤和转换。

## 8.2 食品详情页面

`pages/food/food-detail.vue` 展示食品的完整信息。

### 数据加载

```typescript
const food = ref<FoodItem | null>(null)
const shopStore = useShopStore()

onMounted(async () => {
  const foodId = options?.id || ''
  food.value = await FoodService.getFoodDetail(foodId)
})
```

### 页面结构

```
┌──────────────────────────┐
│      食品图片轮播          │
├──────────────────────────┤
│  名称 / 价格 / 原价       │
│  月销量 / 评分            │
├──────────────────────────┤
│  规格选择（如有）          │
│  [大份 ¥25] [中份 ¥20]   │
├──────────────────────────┤
│  购买数量控制              │
│  [-] 1 [+]               │
├──────────────────────────┤
│  商品描述                 │
├──────────────────────────┤
│      加入购物车按钮        │
└──────────────────────────┘
```

## 8.3 图片轮播

使用 `swiper` 组件展示食品多图：

```html
<swiper class="food-images" indicator-dots indicator-active-color="#FF6B35">
  <swiper-item v-for="img in food.images" :key="img.id">
    <image :src="getImageUrl(img.imageUrl)" mode="aspectFill" class="food-image" />
  </swiper-item>
</swiper>
```

## 8.4 规格选择抽屉（FoodSpecDrawer）

`src/components/food/FoodSpecDrawer.vue` 是一个底部弹出的规格选择面板。

### 规格数据结构

```typescript
interface FoodSpec {
  id: string
  foodId: string
  name: string      // 如 "大份"、"微辣"
  price: number
  sort: number
}
```

### 规格选择逻辑

```typescript
const selectedSpec = ref<FoodSpec | null>(null)

function selectSpec(spec: FoodSpec) {
  selectedSpec.value = spec
}

// 最终价格 = 基础价格 + 规格差价
const finalPrice = computed(() => {
  return selectedSpec.value?.price || food.value?.price || 0
})
```

### 确认加入购物车

```typescript
function confirmAddToCart() {
  if (!selectedSpec.value && food.value?.specs?.length > 0) {
    ToastUtil.showMessage('请选择规格')
    return
  }

  const cartItem: CartFoodItem = {
    ...food.value!,
    buyNum: buyNum.value,
    specId: selectedSpec.value?.id,
    specName: selectedSpec.value?.name,
    price: finalPrice.value
  }

  useCart().addToCart(cartItem)
  show.value = false
}
```

## 8.5 购买数量控制（BuyNumControl）

`src/components/food/BuyNumControl.vue` 提供加减按钮：

```html
<view class="buy-num-control">
  <view class="btn minus" @click="onDecrease" v-if="buyNum > 0">-</view>
  <text class="num" v-if="buyNum > 0">{{ buyNum }}</text>
  <view class="btn plus" @click="onIncrease">+</view>
</view>
```

```typescript
const buyNum = ref(0)

function onIncrease() {
  buyNum.value++
  emit('change', buyNum.value)
}

function onDecrease() {
  if (buyNum.value > 0) {
    buyNum.value--
    emit('change', buyNum.value)
  }
}
```

## 8.6 搜索功能

`src/services/SearchService.ts` 提供搜索能力：

```typescript
export class SearchService {
  /** 搜索食品 */
  static async searchFoods(keyword: string, shopId?: string): Promise<SearchResult> {
    const params: Record<string, unknown> = { keyword }
    if (shopId) params.shopId = shopId
    const response = await httpUtil.get<FoodItem[]>(API_CONFIG.PATHS.SEARCH_FOODS, params)
    const foods = response.data || []
    return {
      foods: foods.map(food => ({
        id: food.id, name: food.name, image: food.image,
        price: food.price, monthlySales: food.monthlySales || 0,
        rating: food.rating || 0, shopId: food.shopId, shopName: ''
      })),
      shops: [], totalCount: foods.length, page: 1, pageSize: 20, hasMore: false
    }
  }

  /** 搜索建议 */
  static async getSuggestions(keyword: string): Promise<string[]> { ... }

  /** 热门搜索 */
  static async getHotSearches(): Promise<string[]> {
    // 有默认兜底数据
    return ['宫保鸡丁', '红烧肉', '鱼香肉丝', '麻婆豆腐', '糖醋排骨']
  }
}
```

搜索页面 `pages/food/search.vue` 包含搜索框、热门搜索、搜索历史和结果列表。

## 小结

本章讲解了食品详情和规格选择的开发。核心难点是**规格选择与价格联动**以及**购物车数据组装**。下一章我们将深入购物车模块。
