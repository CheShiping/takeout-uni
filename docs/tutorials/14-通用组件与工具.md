# 第十四章：通用组件与工具

## 14.1 加载进度条（LoadingProgress）

`src/components/common/LoadingProgress.vue` 是通用的加载指示器，在页面数据加载时展示。

使用场景：
- 店铺列表加载中
- 订单列表加载中
- 食品详情加载中

```html
<LoadingProgress v-if="loading" />
```

## 14.2 Toast 工具（ToastUtil）

`src/utils/ToastUtil.ts` 统一封装 uni-app 的提示 API：

```typescript
export class ToastUtil {
  /** 普通提示 */
  static showMessage(message: string, duration: number = 2000): void {
    uni.showToast({ title: message, icon: 'none', duration })
  }

  /** 成功提示 */
  static showSuccess(message: string): void {
    uni.showToast({ title: message, icon: 'success', duration: 2000 })
  }

  /** 错误提示 */
  static showError(message: string): void {
    uni.showToast({ title: message, icon: 'error', duration: 2000 })
  }

  /** 加载中 */
  static showLoading(message: string = '加载中...'): void {
    uni.showLoading({ title: message, mask: true })
  }

  static hideLoading(): void {
    uni.hideLoading()
  }

  /** 确认对话框（返回 Promise） */
  static showConfirm(title: string, content: string): Promise<boolean> {
    return new Promise((resolve) => {
      uni.showModal({
        title, content,
        success: (res) => resolve(res.confirm)
      })
    })
  }
}
```

使用模式：在 Service/Store 的 try-catch 中配合使用 `showLoading` 和 `hideLoading`。

## 14.3 日志工具（Logger）

`src/utils/Logger.ts` 统一日志前缀，便于调试过滤：

```typescript
export class Logger {
  private static readonly TAG = 'TakeOut'

  static debug(message: string, ...args: unknown[]): void {
    console.debug(`[${Logger.TAG}] ${message}`, ...args)
  }

  static info(message: string, ...args: unknown[]): void {
    console.info(`[${Logger.TAG}] ${message}`, ...args)
  }

  static warn(message: string, ...args: unknown[]): void {
    console.warn(`[${Logger.TAG}] ${message}`, ...args)
  }

  static error(message: string, ...args: unknown[]): void {
    console.error(`[${Logger.TAG}] ${message}`, ...args)
  }
}
```

在浏览器控制台中搜索 `[TakeOut]` 即可过滤应用日志。

## 14.4 日期工具（DateUtil）

`src/utils/DateUtil.ts` 处理各种日期格式：

```typescript
export class DateUtil {
  /** 格式化为 yyyy-MM-dd HH:mm:ss */
  static formatTime(timeStr: string): string { ... }

  /** 仅日期 yyyy-MM-dd */
  static formatDateOnly(timeStr: string): string { ... }

  /** 仅时间 HH:mm:ss */
  static formatTimeOnly(timeStr: string): string { ... }

  /** 相对时间（"刚刚"、"5分钟前"、"3天前"） */
  static formatRelativeTime(timeStr: string): string {
    const diff = now.getTime() - date.getTime()
    const seconds = Math.floor(diff / 1000)
    const minutes = Math.floor(seconds / 60)
    const hours = Math.floor(minutes / 60)
    const days = Math.floor(hours / 24)

    if (seconds < 60) return '刚刚'
    if (minutes < 60) return `${minutes}分钟前`
    if (hours < 24) return `${hours}小时前`
    if (days < 30) return `${days}天前`
    return DateUtil.formatDateOnly(timeStr)
  }
}
```

兼容处理：支持秒级时间戳、毫秒级时间戳和日期字符串三种输入。

## 14.5 数学工具（MathUtil）

`src/utils/MathUtil.ts` 解决 JavaScript 浮点数精度问题：

```typescript
export class MathUtil {
  static addition(a: number, b: number): number {
    const factor = MathUtil._getFactor(a, b)
    return (a * factor + b * factor) / factor
  }

  static subtraction(a: number, b: number): number { ... }
  static multiply(a: number, b: number): number { ... }
  static divide(a: number, b: number): number { ... }

  /** 保留小数位 */
  static toFixed(num: number, decimalPlaces: number = 2): number {
    const factor = Math.pow(10, decimalPlaces)
    return Math.round(num * factor) / factor
  }
}
```

原理：将浮点数转为整数运算后再转回，避免 `0.1 + 0.2 !== 0.3` 的问题。

## 14.6 存储工具（StorageUtil）

`src/utils/StorageUtil.ts` 封装 uni-app 的本地存储：

```typescript
export class UniStorage {
  static getToken(): string {
    return uni.getStorageSync('token') || ''
  }

  static setToken(token: string): void {
    uni.setStorageSync('token', token)
  }

  static clearToken(): void {
    uni.removeStorageSync('token')
  }

  /** 通用存取 */
  static getItem<T>(key: string): T | null {
    try {
      const value = uni.getStorageSync(key)
      return value ? JSON.parse(value) as T : null
    } catch {
      return uni.getStorageSync(key) as unknown as T
    }
  }

  static setItem(key: string, value: unknown): void {
    if (typeof value === 'string') {
      uni.setStorageSync(key, value)
    } else {
      uni.setStorageSync(key, JSON.stringify(value))
    }
  }
}
```

## 14.7 敏感词过滤（SensitiveFilter）

`src/utils/SensitiveFilter.ts` 用于评价内容审核：

```typescript
export class SensitiveFilter {
  private static readonly SENSITIVE_WORDS = [
    '垃圾', '难吃', '恶心', '骗子', '假货',
    '黑心', '投诉', '举报', '差评', '退款'
  ]

  static hasSensitiveWord(text: string): boolean
  static filter(text: string): string          // 替换为 *
  static getSensitiveWords(text: string): string[]
  static validateContent(content: string): { valid: boolean; message: string }
}
```

## 小结

本章讲解了 7 个工具类的设计。这些工具类遵循**单一职责 + 静态方法**的模式，在整个项目中被广泛复用。好的工具类能显著减少重复代码。下一章我们将讲解跨平台适配。
