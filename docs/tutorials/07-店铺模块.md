# 第七章：店铺模块

## 7.1 店铺数据模型

```typescript
// src/models/shop.types.ts

export interface Shop {
  id: string
  name: string
  logo: string
  rating: number
  monthlySales: number
  address: string
  longitude: number
  latitude: number
  phone: string
  businessHours: string
  deliveryFee: number
  minOrderAmount: number
  description: string
  status: string
}

export interface NearbyShop extends Shop {
  distance: number  // 距离（米）
}
```

## 7.2 ShopService 服务层

```typescript
// src/services/ShopService.ts

export class ShopService {
  /** 获取所有店铺 */
  static async getShops(): Promise<Shop[]> {
    const response = await httpUtil.get<Shop[]>(API_CONFIG.PATHS.SHOP_LIST)
    return response.data || []
  }

  /** 获取店铺详情 */
  static async getShopDetail(shopId: string): Promise<Shop> {
    const url = API_CONFIG.PATHS.SHOP_DETAIL.replace('{id}', shopId)
    const response = await httpUtil.get<Shop>(url)
    return response.data
  }

  /** 按名称获取店铺（本地筛选） */
  static async getShopByName(name: string): Promise<Shop | null> {
    const shops = await ShopService.getShops()
    return shops.find(s => s.name === name) || shops[0] || null
  }

  /** 获取附近店铺 */
  static async getNearbyShops(latitude: number, longitude: number, radius?: number): Promise<NearbyShop[]> {
    const params: Record<string, unknown> = { latitude, longitude }
    if (radius) params.radius = radius
    const response = await httpUtil.get<NearbyShop[]>(API_CONFIG.PATHS.SHOP_NEARBY, params)
    return response.data || []
  }
}
```

## 7.3 店铺列表页面

`pages/shop/shop-list.vue` 展示附近店铺列表。

### 加载数据

```typescript
const shops = ref<Shop[]>([])
const loading = ref(false)

onMounted(async () => {
  loading.value = true
  try {
    const shopStore = useShopStore()
    if (shopStore.shops.length === 0) {
      await shopStore.fetchShops()
    }
    shops.value = shopStore.shops
  } finally {
    loading.value = false
  }
})
```

### 店铺卡片设计

每个店铺卡片包含：
- 店铺 Logo（左侧）
- 店铺名称、评分、月销量（右侧上方）
- 地址、营业时间、配送费（右侧下方）
- 点击跳转到店铺详情

```typescript
function onShopClick(shop: Shop) {
  const shopStore = useShopStore()
  shopStore.setCurrentShop(shop)
  uni.navigateTo({ url: `/pages/food/food-detail?shopId=${shop.id}` })
}
```

## 7.4 地理位置服务

`src/hooks/useLocation.ts` 封装了获取位置的逻辑：

```typescript
export function useLocation() {
  const latitude = ref<number>(0)
  const longitude = ref<number>(0)
  const loading = ref(false)

  async function getLocation(): Promise<boolean> {
    loading.value = true
    try {
      const res = await uni.getLocation({
        type: 'wgs84',
        isHighAccuracy: true
      })
      latitude.value = res.latitude
      longitude.value = res.longitude
      return true
    } catch (err) {
      ToastUtil.showError('获取位置失败')
      return false
    } finally {
      loading.value = false
    }
  }

  return { latitude, longitude, loading, getLocation }
}
```

### 地图工具（MapUtil）

`src/utils/MapUtil.ts` 提供坐标转换和距离计算：

```typescript
export class MapUtil {
  /** WGS84 转 GCJ02（火星坐标系） */
  static wgs84ToGcj02(lng: number, lat: number): { longitude: number; latitude: number } {
    // ... 偏移算法
  }

  /** Haversine 公式计算两点距离（米） */
  static calculateDistance(lat1: number, lng1: number, lat2: number, lng2: number): number {
    const dLat = MapUtil._toRadians(lat2 - lat1)
    const dLng = MapUtil._toRadians(lng2 - lng1)
    const a = Math.sin(dLat / 2) ** 2 +
      Math.cos(MapUtil._toRadians(lat1)) * Math.cos(MapUtil._toRadians(lat2)) *
      Math.sin(dLng / 2) ** 2
    const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a))
    return MapUtil.EARTH_RADIUS * c
  }

  /** 格式化距离显示 */
  static formatDistance(meters: number): string {
    if (meters < 1000) return Math.round(meters) + 'm'
    return (meters / 1000).toFixed(1) + 'km'
  }
}
```

## 7.5 ShopStore 与数据流

店铺数据通过 `useShopStore` 管理，支持多种查询方式：

```typescript
// 全局共享店铺列表
const shopStore = useShopStore()
await shopStore.fetchShops()           // 加载所有店铺
await shopStore.fetchShopDetail(id)    // 加载详情
shopStore.setCurrentShop(shop)         // 手动设置当前店铺
```

典型数据流：`ShopList 页面 → ShopStore → ShopService → API`

## 小结

本章讲解了店铺模块的开发。核心是**Service 静态方法 + Store 状态管理 + Hook 组合式函数**的三层协作。下一章我们将讲解食品详情与规格选择。
